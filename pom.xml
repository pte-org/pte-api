<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.5</version>
		<relativePath/>
	</parent>

	<groupId>com.pte</groupId>
	<artifactId>pte-api-parent</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>pte-api-parent</name>
	<description>PTE microservice platform — mono-repo aggregator (ADR-001/002/003/004)</description>

	<properties>
		<java.version>21</java.version>
	</properties>

	<!-- gateway-removal (follow-up to plans/modular-monolith Phase 11 cutover):
	     `gateway` is gone — CORS and per-tenant rate limiting were ported into
	     `app`'s own SecurityConfig (com.pte.identity.internal.config), and
	     Nginx now routes straight to `app`, preserving the versioned /api/v1/**
	     path. `pte-common` is gone with it: gateway was its
	     only consumer in this reactor (app deliberately never depended on it —
	     see app/pom.xml's comment on Spring Modulith module scanning; its
	     classes were ported into app's own com.pte.shared package instead). -->
	<modules>
		<module>app</module>
	</modules>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<configuration>
						<annotationProcessorPaths>
							<path>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
							</path>
						</annotationProcessorPaths>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-maven-plugin</artifactId>
					<configuration>
						<excludes>
							<exclude>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
							</exclude>
						</excludes>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

</project>
