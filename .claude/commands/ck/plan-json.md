---
description: Generate a phased JSON plan bundle. --spec <path> is preferred but optional; inline feature description is the fallback. --output <path> selects the master plan.json entry point (default plans/<slug>/plan.json).
---

Load the `ck:plan-json` skill and run it with `$ARGUMENTS`.

Phase JSON files are siblings beside the master `plan.json` selected by `--output`.
