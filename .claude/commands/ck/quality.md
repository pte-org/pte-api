---
description: Independent quality auditor. `--gate <phase-file>` blocks a ck:cook phase on architecture/ownership/maintainability findings. `--audit <path>`, `--diff <ref>`, or `--changed` audit any target standalone (report-only unless --save). `--verify <report>` re-checks prior findings. Never edits code or writes tests.
---

Load the `ck:quality` skill and run it with `$ARGUMENTS`.
