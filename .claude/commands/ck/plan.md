---
description: Plan a feature or system with auto-detected complexity. Modes (pick one): --fast (quick plan → cook), --hard (research + red-team + validate). Composable flag (any mode): --tdd (tests-first per phase). Every phase gets Design Constraints and Quality/Testing State for the mandatory cook quality gate. Always starts with Scope Challenge.
---

Load the `ck:plan` skill and run it with `$ARGUMENTS`.
