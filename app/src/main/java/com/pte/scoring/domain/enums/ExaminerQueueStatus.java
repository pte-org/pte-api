package com.pte.scoring.domain.enums;

/** Progress filter derived from the submitted Examiner scores for one assigned attempt. */
public enum ExaminerQueueStatus {
    ALL,
    PENDING,
    IN_PROGRESS,
    COMPLETED
}
