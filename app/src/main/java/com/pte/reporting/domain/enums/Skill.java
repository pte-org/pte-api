package com.pte.reporting.domain.enums;

/**
 * The 4 PTE communicative skills reported to students (spec Out of Scope:
 * enabling skills — Grammar/Oral Fluency/Pronunciation/Spelling/
 * Vocabulary/Written Discourse — are no longer reported; the pinned
 * ScoreTemplate has no weight column for them).
 */
public enum Skill {
    LISTENING,
    READING,
    SPEAKING,
    WRITING
}
