package com.pte.session.domain.enums;

/** Host intent at session creation; resolves to a default {@link com.pte.session.domain.ExamPolicy}. */
public enum ExamMode {
    PRACTICE,
    MOCK_TEST,
    REAL_EXAM
}
