package com.pte.session.domain.enums;

/** STRICT's concrete encryption/hash mechanism belongs to attempt/scoring; session only carries the setting. */
public enum AnswerIntegrityLevel {
    STANDARD,
    STRICT
}
