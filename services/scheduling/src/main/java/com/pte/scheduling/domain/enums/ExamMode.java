package com.pte.scheduling.domain.enums;

/** Host intent at session creation; resolves to a default {@link com.pte.scheduling.domain.ExamPolicy}. */
public enum ExamMode {
    PRACTICE,
    MOCK_TEST,
    REAL_EXAM
}
