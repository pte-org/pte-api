package com.pte.scheduling.domain.enums;

/** STRICT's concrete encryption/hash mechanism is task 20's scope; this service only carries the setting. */
public enum AnswerIntegrityLevel {
    STANDARD,
    STRICT
}
