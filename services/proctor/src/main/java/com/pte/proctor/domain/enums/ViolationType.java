package com.pte.proctor.domain.enums;

/** Fixed vocabulary a proctor console offers when flagging an attempt — Milestone 1 scope (no automated detection). */
public enum ViolationType {
    TAB_SWITCH,
    MULTIPLE_FACES,
    FACE_NOT_VISIBLE,
    SUSPICIOUS_AUDIO,
    TECHNICAL_ISSUE,
    OTHER
}
